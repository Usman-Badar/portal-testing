import React, { lazy,} from 'react';
const UI = lazy(() => import('./UI'));

const TempEmployees = () => {

    return (
        <>
            <UI />
        </>
    )
}
export default TempEmployees;